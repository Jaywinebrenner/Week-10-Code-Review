<div class="header-top">
  <h1 class="header1">Mario's</h1>
  <h2 class="header2">Discount Specialty Foods</h2>
  <%= image_tag "mario.png" %>
</div>
<hr>
<div class="container id="about">
  <div class="row">
    <div class="col-lg-12">
      <h5>Mario's Discount Specialty Foods specializes in specialty foods that are discounted. Mario provides tastebud-tickling specialty ingredients at discoutned rates which will reverse any trauma your checkbook may be experiencing. Please support Mario's Discount Specialty Foods by purchasing Mario's discounted products that are guarenteed to be edible.</h5>
      <hr>
      <!-- <form>
        <label for="">Search Mario's Products:</label>
        <input type="text" name="search">
        <button type="submit" name="button">go</button>
      </form> -->
    </div>
  </div>
</div>

<div class="header-middle">
  <h2>Have a Look at Mario's Product Line!</h2>
</div>

<div class="container">
  <div class="row">

    <div class="col-lg-3">
      <h3>All Products Available</h3>
      <h4><%= link_to "Create new product", new_product_path %></h4>

      <% if @products.any? %>
      <ul>
        <% @products.each do |product| %>
        <li><%= link_to product.name, product_path(product) %></li>
        <% end %>
      </ul>
      <% else %>
      <p>There are no products yet.</p>
      <% end %>
    </div>

    <div class="col-lg-3">
      <h3>Products made in the USA</h3>
      <% if @usa_products.any? %>
      <ul>
        <% @usa_products.each do |product| %>
        <li><%= link_to product.name, product_path(product)%></li>
        <% end %>
      </ul>
      <% else %>
      <p>There are no products that are made in the USA in stock.</p>
      <% end %>
    </div>

    <div class="col-lg-3">
      <h3>The Three Most Recently Added Products</h3>
      <% if @three_most_recent.any? %>
      <ul>
        <% @three_most_recent.each do |product| %>
        <li><%= link_to product.name, product_path(product)%></li>
        <% end %>
      </ul>
      <% else %>
      <p>Looks like we don't have much in stock.</p>
      <% end %>
    </div>
    <div class="col-lg-3">
      <h3>Mario's Most Reviewed Product</h3>
    </div>
  </div>
</div>
<hr>




<div class="scope-div">





</div>
